let a = [12,23,12,120,121];

//a.pop();//for delete the last  element in array

//a.push(1211);//for add a new element in the array

//a.shift();//for delete first element in the array and shift other element //to the left

//a.unshift(133);//add element in the 0 index and shift other element to the //right 

//a.splice(3,1);//it used to remove an array element of any position by giving (index,position) in splice methode 

//a.sort();//this methode use for sorting array by their first value

//a.sort(function(a, b){return a - b});//this methode use for sorting numeric array correctly

//a.reverse();// this methode use for reverse all array element

console.log(a);

for(let b=0; b<a.length ; b++){

console.log("the element is "+ a[b]);

}

